# Pinned to the versions this repository was developed and validated against.
# Loosen these (>= instead of ==) if you run into resolver conflicts, but the
# BoTorch / GPyTorch / pyro-ppl trio is the most likely to need pinning.

# Core scientific stack
numpy==1.26.4
pandas==2.3.3
# scipy>=1.17.0 is required by matminer 0.10.1. This file previously pinned
# 1.16.3, which pip cannot resolve — `pip install -r requirements.txt` failed
# outright on a clean environment even though the author's conda env ran fine
# (conda installed scipy without enforcing matminer's pip metadata). 1.17.1
# needs numpy>=1.26.4,<2.7, which the numpy pin above satisfies exactly; 1.18
# would force numpy>=2.0 and break it.
scipy==1.17.1
scikit-learn==1.8.0
# Parquet engine for pandas. Every pipeline stage caches through .parquet
# (raw, featurized, candidates, both parity files — 18 call sites), so this is
# a hard runtime dependency, not an optional accelerator. It was never
# declared: it happened to be present in the author's conda env as a
# transitive dependency, so step 1 failed at the first `to_parquet` for anyone
# installing from this file.
pyarrow==24.0.0

# Plotting
matplotlib==3.10.0
seaborn==0.13.2
plotly==6.7.0

# Materials-science featurization
matminer==0.10.1
pymatgen==2026.3.23
smact==4.0.0

# Surrogates: exact GP + multi-task GP + BNN
torch==2.12.1
gpytorch==1.15.2
botorch==0.18.1
pyro-ppl==1.9.1

# XGBoost (optional; used by auto_eda's baseline models)
xgboost==3.2.0

# Local LLM (optional; step 6 report and auto_eda ensemble)
ollama==0.6.2

# Report templating
jinja2==3.1.6

# Web UI
gradio==6.19.0
gradio_client==2.5.0

# Test suite (`python -m pytest tests/`)
pytest==9.1.1
